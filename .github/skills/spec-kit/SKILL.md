---
name: spec-kit-project
description: Apply the pinned Spec Kit workflow for changes to this repository.
---

# Spec Kit 1.0.6

Read `.specify/memory/constitution.md` and the active feature under `specs/` completely. Follow `spec → plan → tasks → implement`; update the existing artifact before code when requirements or architecture change. Keep real beneficiary data outside Git. Finish by updating `tasks.md` and running the verification commands required by `.github/copilot-instructions.md`.
